package cad.native_

/**
 * TODO(jextract): сюда лягут сгенерированные jextract-биндинги для manifoldc.
 * См. план в [cad.kernel.manifold.ManifoldKernel].
 *
 * Структура после генерации:
 *   cad.native_.manifold.Manifoldc          — главный класс с downcall-методами
 *   cad.native_.manifold.manifold_op_t      — opaque-struct handle
 *   cad.native_.manifold.manifold_polygons_t — ...
 */
internal object NativePlaceholder
